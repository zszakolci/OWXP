{namespace WeightedScreen}

/**
 * Prints the Weighted Screen portlet main view.
 *
 * @param id
 */
{template .render}
	<div id="{$id}" style="position: relative; height: 100%">
		<div id="weightedscreen" style="min-height: 500px">
		</div>
	</div>
{/template}